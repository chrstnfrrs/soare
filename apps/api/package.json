{
  "name": "soare-api",
  "version": "0.1.0",
  "description": "n/a",
  "main": "src/index.js",
  "author": "chrstnfrrs",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc && concurrently \"tsc -w\" \"nodemon --es-module-specifier-resolution=node --watch build build/index\"",
    "db:generate": "prisma generate",
    "db:migrate:dev": "prisma migrate dev"
  },
  "dependencies": {
    "@graphql-tools/schema": "^8.3.1",
    "@prisma/client": "^3.9.1",
    "apollo-server": "^3.6.2",
    "graphql": "^16.3.0"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.15",
    "prisma": "^3.9.1",
    "typescript": "^4.5.5"
  }
}
